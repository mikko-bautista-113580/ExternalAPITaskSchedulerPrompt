# Per-run task logs (rotated locally, not source)
logs/
**/logs/

# Runtime lock files held by the wrappers
*.lock

# Per-machine path overrides (each dev copies config.local.example.json -> config.local.json)
config.local.json
